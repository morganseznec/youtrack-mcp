__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# distribution / packaging
build/
dist/
*.egg-info/
*.egg

# environments
.venv/
venv/
env/
.python-version

# uv
uv.lock

# editors
.vscode/
.idea/
*.swp
*~
.DS_Store

# local config (token in env, don't commit accidentally if a .env exists)
.env
.env.local

# local dogfooding config: the maintainer's own .youtrack.yml that points to
# their personal YouTrack instance. Users SHOULD commit a .youtrack.yml in
# their own repos; this gitignore only applies to this MCP repo.
.youtrack.yml
.youtrack.yaml
